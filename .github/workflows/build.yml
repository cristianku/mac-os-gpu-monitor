name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build gpu-monitor
        run: make build
      - name: Smoke test
        run: ./build/gpu-monitor --version
